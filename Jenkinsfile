pipeline {
    agent {
        node {
            label "docker-builder"
        }
    }
    stages {
        stage('build') {
            steps {
                echo 'YOY'
                sh 'node -v'
            }
        }
    }
}