pipeline {
    agent { docker { image 'node:14-alpine' } }
    stages {
        stage('build') {
            steps {
                echo 'YOY'
                sh 'node --version'
                echo 'WOOORk'
            }
        }
    }
}